/**
 * Типизированная конфигурация (master plan разд.6.2) — заготовка.
 * Наполнение (env-schema validation через zod/joi, per-app config типы)
 * добавляется по мере роста числа переменных окружения за пределы
 * того, что apps/api/src/main.api.ts читает напрямую сейчас. Не входит
 * в этот первый проход C-01/C-03 как отдельная задача.
 */
export {};
