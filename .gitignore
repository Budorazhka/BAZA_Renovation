node_modules/
dist/
.next/
.turbo/
coverage/
*.log
.env
.env.local
.env.*.local
.DS_Store
*.tsbuildinfo

# Playwright D-07 runtime E2E gate artifacts (apps/e2e-runtime) — traces/
# screenshots/videos/HTML report, all regenerated per run, retain-on-failure
# only per playwright.config.ts, never meant to be committed.
/apps/e2e-runtime/playwright-report/
/apps/e2e-runtime/test-results/

# apps/erp-web — ВЕНДОРЕННАЯ копия ERP-клиента (02.09.2026).
#
# Раньше здесь лежал чекаут чужого репозитория Berega-team/bz26-client-erp, и
# путь игнорировался. Теперь это копия внутри нашего монорепо: правки делаются
# здесь и коммитятся сюда, в чужой репозиторий из работы над BAZA_Renovation мы
# не пушим. Поэтому запись об игнорировании снята — иначе работа снова
# оказалась бы вне версионирования.
#
# Оригинал остаётся нетронутым в working/bz26-client-erp-main (только чтение).

# Локальные артефакты инструмента second-opinion — не часть проекта.
/.second-opinion/
