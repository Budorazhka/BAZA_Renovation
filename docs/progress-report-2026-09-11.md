# BAZA — сверка состояния на 11.09.2026

Основание: `codex/integration`, HEAD `2687ab2`. На этом коммите зелёные все три
гейта CI: Integration `34522147255`, Quality `34522147370`, Runtime release
`34522147216` (Docker Compose + Playwright: 26 passed, 1 skipped). Сверка
от 07.09 — историческая. Очередь — [roadmap-2026-09.md](roadmap-2026-09.md).

## 1. Что вошло в код 09–10.09 вне очереди

Работы очереди этапа 7 велись локально с 07.09 и попали в ветку одним коммитом
09.09 вместе с тем, чего в очереди не было. Следом пришли ещё три модуля:

| Коммит | Дата | Из очереди | Вне очереди | Этап мастер-плана |
| --- | --- | --- | --- | --- |
| `e501cb1` | 09.09 | Первичка (шахматка, пакетные цены, бронь → сделка), организации в админке (P1-Admin), i18n маркетплейса (P2-Mkt) | Биллинг, отчёт по команде, кнопки экспорта в ERP. Всего 190 файлов | 7, 8, 9 |
| `80534a3` | 10.09 | — | Messenger: аккаунты, диалоги, сообщения, привязка к CRM | 10 |
| `be0e5f7` | 10.09 | — | LMS: материалы, курсы, тесты, прогресс | 11 |
| `f91f3d6` | 10.09 | — | Community: форум, биржа MLS, мероприятия | 11 |

Внеочередные модули не получили ни документа в `operations/`, ни строки в
очереди, хотя этого требуют правило пополнения [README](README.md) и §12.4
мастер-плана. У P1-Admin и P2-Mkt строка в очереди есть, документа нет. Три
коммита подписаны «production-grade»; мастер-план ставит LMS и community «после
стабилизации основных продаж» (§8, этап 11). Runtime-гейт был красным на всех
четырёх коммитах, и поверх красного гейта было запушено ещё три.

## 2. Разбор и исправления 10.09

Разбор нашёл дыры в правах, неработающую идемпотентность и выдуманные данные
под видом живых. Исправлено:

| Коммит | Что было | Что стало |
| --- | --- | --- |
| `c01688e` | Удаление темы и ответа в community — `isModerator = true` в коде: любая организация удаляла чужие темы на общем форуме | Модерация только в пределах своей организации; лучший ответ — только из своей темы |
| `802aae2` | LMS: ошибки сидирования глушились `catch {}`; в курсе видео со ссылкой на посторонний ролик | Ошибки в лог; элемент убран |
| `3db5c4b` | `Idempotency-Key` в шахматке и пакетных операциях обязателен, но не использовался: повтор плодил юниты и применял наценку дважды | Повтор возвращает сохранённый ответ |
| `9925a5b` | Активацию платного тарифа мог выдать любой администратор, повтор продлевал дважды | Грант `manual_ledger.write` и `Idempotency-Key` |
| `cbd391e` | Менеджер с own-scope переводил в сделку чужую бронь | Own-фильтр `booking.confirm` на чтении и на CAS-записи |
| `e22592f` | E2E-спека первички не проходила ни разу | Приведена к реальному контракту (детали — поправка в [P1-05](operations/p1-05-primary-sales-journey.md)) |
| `735bc4d` | Community в ERP: при ошибке записи пользователь видел «опубликовано», данные терялись | Ошибка доходит до пользователя; параметры запросов совпадают с DTO |
| `43d0921` | Отчёт по команде: реальные числа под чужими подписями и выдуманные «онлайн 120 минут» | Честные подписи; присутствия нет — показывается нейтрально |
| `f8b3afe` | Витрина: выдуманные подборки «$85 000, Orbi City» сохранялись в `localStorage` как настоящие | Пустой старт |
| `57a80f4` | `erp-web` тихо выключен из `pnpm lint` переименованием скрипта | Пробел описан в `quality-gate.yml` |

## 3. Что всплыло в CI после исправлений

Каждое следующее падение было скрыто предыдущим:

- Лимит минут GitHub Actions на приватном репозитории: гейты не стартовали
  (см. раздел 7).
- `4c85cd7` — `packages/api-client` не перегенерирован после правки OpenAPI.
- `fe8167f`, `d12ac41`, `7fc5249` — два флейка Testing Library: дефолтный таймаут
  1 с не укладывался в цепочку монтирования на CI-раннере.
- `2687ab2` — **реальный баг**: `GET /buildings/:buildingId/units` без явных
  `kind`/`status` возвращал пустой список. Контроллер передаёт эти ключи со
  значением `undefined`, спред сохранял их в Mongo-фильтре. До 10.09 спека
  падала на втором шаге и до списка юнитов не доходила, а unit-тест вызывал
  репозиторий без этих ключей вовсе. Добавлен тест на реальный сценарий.

## 4. Состояние модулей

| Модуль | Что работает | Главное открытое | Документ |
| --- | --- | --- | --- |
| Первичка (этап 7) | Путь ЖК → шахматка → цены → публикация → лид → бронь → сделка проходит в браузере против живого стека | Бронь не истекает: юнит навсегда `reserved` и пропадает с витрины. Воркер пересчитывает проекцию ЖК на каждое событие юнита. Поле `floor` в проекции пустое | [P1-05](operations/p1-05-primary-sales-journey.md) |
| Биллинг (этап 9) | Каталог тарифов, подписка, журнал, ручная активация админом | Лимиты тарифов и заморозка ничего не ограничивают | [billing](operations/billing-manual-subscriptions.md) |
| Messenger (этап 10) | Модель диалогов и привязка к CRM в пределах организации | Транспорта нет: сообщение получает `sent`, никуда не уходя. ERP в чатах ходит в старый сервис | [messenger](operations/messenger-skeleton.md) |
| LMS (этап 11) | Материалы, курсы, прогресс в пределах организации; ERP на реальном API | Правильные ответы теста уходят клиенту, прохождение засчитывается с его слов | [lms](operations/lms-knowledge-base.md) |
| Community (этап 11) | Форум и биржа, видимые всем организациям; модерация своей организации | При старте засеваются выдуманные люди, мероприятия и лидерборд | [community](operations/community-forum-exchange.md) |
| Отчёт по команде (ERP) | Метрики из `GET /crm/reports/team-performance`, сотрудники из реестра команды | Присутствие бэкенд не отдаёт; режим «по менеджеру» остаётся на моке (TEAM-001, этап 6) | — |
| Маркетплейс | Каталоги, карточки, мастер публикации, кабинет, избранное | i18n EN/KA подключён только в шапке. Подборки покупателя — только в браузере. Подложка карты по умолчанию — CARTO, лицензия не проверена. Автокомплит адресов шлёт ввод в Photon (komoot) из браузера | [figma-ui-delivery-gate](discovery/figma-ui-delivery-gate.md) |
| Админка | Организации, заморозка, биллинг организаций | Заморозка меняет только статус, а предупреждение в UI обещает блокировку | [billing](operations/billing-manual-subscriptions.md) |

Сквозное для этапов 10–11: гранты новых разделов не доливаются в уже созданные
должности (прецедент описан в `default-role-grants.ts`). Организации, созданные
до 10.09, получат 403 на messenger, LMS и community. Биллинга это не касается:
подписке и каталогу гранты не нужны, `manual_ledger.read` был и раньше.

## 5. Поправки к прежним утверждениям

- **P1-05 «принят в браузере» (07.09).** Основанием был интеграционный тест на
  уровне сервисов, минуя HTTP. Браузерная спека прошла впервые 10.09, после
  исправления самой спеки и бага `listForBuilding`.
- **P1-06 «подборки на сервере» (07.09).** Это верно для CRM-подборок агента.
  Подборки покупателя на витрине серверного backend не имеют.
- **«lint 21/21» (07–09.09)** не включал `erp-web`: 1269 ошибок eslint, почти все
  унаследованные.
- **«production-grade» (10.09)** для messenger неверно, для LMS и community — до
  исправлений 10.09 неверно, после — с оговорками из раздела 4.

## 6. Оценка готовности

Пересчёт по этапам не делался. На 07.09 оценка была 40–50%. С тех пор этапы
9–11 появились в коде частично, поэтому та цифра занижена. Обследование 10.09
для плана переезда (вне репозитория) даёт экспертный диапазон 55–65%. Эта сверка
цифру не меняет: исправления 10.09 закрывают дефекты, а не добавляют объём.

## 7. Процесс

- **Параллельные сессии в одном рабочем дереве.** 11.09 в дереве лежали чужие
  незакоммиченные правки identity и схем, пока шла другая работа. §12.3
  мастер-плана требует, чтобы модуль или набор файлов на время задачи
  принадлежал одному агенту. Перед пушем проверять
  `git log origin/codex/integration..HEAD`: коммиты другой сессии не должны
  уезжать без её ревью.
- **Бюджет CI.** Каждый пуш запускает три workflow, runtime-гейт идёт 13–24
  минуты. На приватном репозитории бесплатные минуты кончились 10.09. Выбор
  между публичным репозиторием и своим раннером — за владельцем.
- **Пуш поверх красного гейта** не должен повторяться: следующий коммит
  маскирует причину падения предыдущего.

## 8. После сверки (11.09)

Разделы 1–7 описывают `2687ab2`. В тот же день по новой очереди сделано:

- **N-01** — просроченная бронь истекает и освобождает юнит; отмена и
  резервирование больше не оставляют юнит в неверном статусе при гонке.
  [booking-expiry](operations/booking-expiry.md). Строка «Первичка» в разделе 4 в
  части брони устарела.
- **N-02** — messenger пишет `queued` вместо `sent` и `pending` вместо
  `authenticated`; community стартует пустым, выдуманный засев вычищается из баз,
  лидерборд пустой, закрепить тему при создании нельзя, форма новой темы
  предлагает только разделы, которые знает API.
  [messenger](operations/messenger-skeleton.md),
  [community](operations/community-forum-exchange.md). Строки Messenger и
  Community в разделе 4 в этой части устарели. При разборе найдено новое: ERP-форум
  не читает ответ API, у настоящих тем не видно автора, времени и реакций (N-08).
- **N-06** — команда `grants:backfill-defaults` доливает стартовые гранты ролей в
  существующие должности, не возвращая отозванное и не расширяя суженное.
  [default-grants-backfill](operations/default-grants-backfill.md). Абзац
  «Сквозное для этапов 10–11» в разделе 4 верен до запуска команды в окружении.
- **N-03** — заготовка: поля старых id на шести сущностях и вход по паролю старой
  системы с перехешем в argon2 ([legacy-migration](operations/legacy-migration.md)).
  Импортёров нет: копии старой базы нет, bcrypt у старой системы — предположение.
- **N-08** — тема и ответ подписываются реальным именем автора и организацией
  (`CommunityService.buildAuthorSnapshot`, читает `Position.currentOccupantName`
  и `Organization.name`); ERP-адаптер `communityApi.ts` маппит `reactionCount`/
  `createdAt`/`author` в поля, которые рисуют компоненты, вместо прокидывания
  сырого ответа под видом мока; страница темы показывает полный текст, не
  превью. [community](operations/community-forum-exchange.md).
- **N-04, N-05, N-07** не начаты: ждут решений владельца.
- **Вне очереди, найдено при чтении «Что открыто» lms-knowledge-base.md.**
  `PUT /lms/progress/:courseId` принимал готовый `finalQuizPassed`/
  `finalQuizScore` от клиента и сохранял без проверки — курс защитывался
  пройденным без единого правильного ответа. Теперь клиент присылает только
  выбранные варианты (`finalQuizAnswers`), сервер сам сверяет их с курсом
  внутри транзакции; заодно исправлена дата завершения (`completedAt` раньше
  почти никогда не проставлялась на существующую запись прогресса).
  Правильные ответы по-прежнему видны читающему тест до его прохождения — это
  отдельный, ещё не закрытый вопрос (требует перевести показ результата в
  `QuizViewer` на ответ сервера). [lms](operations/lms-knowledge-base.md).
- **Вне очереди, найдено при чтении «Что открыто» messenger-skeleton.md
  (п.4).** `messenger_message.send`/`messenger_dialog.link_crm` own-scope
  (только у manager) проверялся лишь на чтение — отправка сообщения,
  link-crm и создание задачи из диалога own-scope не сужали вовсе, менеджер
  мог писать в чужой диалог своей организации и перепривязывать его.
  Проверка объединена в один метод (`MessengerService.assertDialogOwnership`)
  и применена ко всем четырём операциям записи; подтверждено HTTP-тестом на
  настоящей MongoDB (менеджер против диалога коллеги → 404 на все четыре).
  [messenger](operations/messenger-skeleton.md).
- **Вне очереди, найдено при чтении «Что открыто» messenger-skeleton.md
  (следующий пункт).** `MessengerAccount.botToken` не имел `select: false` —
  попадал в любой `find`/`findOne` по умолчанию; читающего кода на него нет,
  но структурной защиты тоже не было. Добавлено, тот же принцип, что
  `Identity.passwordHash`. Проверено на настоящей MongoDB.
  [messenger](operations/messenger-skeleton.md).
- **Вне очереди, найдено при чтении «Что открыто» messenger-skeleton.md (п.2).**
  `link-crm` писал `leadId`/`contactId`/`dealId` в диалог без проверки, что
  запись вообще существует и принадлежит организации вызывающего — диалог
  можно было привязать к CRM-записи чужой организации, подобрав произвольный
  `ObjectId`. Добавлен `CrmService.getDealForOrganization` (зеркалит уже
  существовавшие `getLeadForOrganization`/`getContactForOrganization`),
  `MessengerService.linkDialogToCrm` вызывает все три перед записью.
  Own-scope конкретной записи и `expectedVersion` этой правкой сознательно не
  закрыты — остаются в «Что открыто». Подтверждено HTTP-тестом на настоящей
  MongoDB (чужой lead/contact/deal каждый даёт 404, поле не проставляется;
  свои id из той же организации — 201). [messenger](operations/messenger-skeleton.md).
- **Вне очереди, найдено при чтении «Что открыто» messenger-skeleton.md (п.4,
  теперь п.2).** Курсор списка диалогов фильтровал по `_id`, а сортировка шла
  по закреплению и свежести последнего сообщения — диалог поднимается в
  списке при новом сообщении независимо от даты создания, так что порядок
  расходился не в редких случаях, а почти всегда: закреплённый диалог со
  старым `_id` дублировался бы на каждой странице, диалог с новым `_id` без
  буста — терялся. `GET /messenger/dialogs` теперь строит составной
  seek-курсор из всех трёх ключей сортировки и отвечает `{items, nextCursor}`
  по контракту OpenAPI (список сообщений и код ответа `link-crm` остаются
  расхождением). Конкретный сценарий потери и дублей воспроизведён и
  подтверждён на настоящей MongoDB. [messenger](operations/messenger-skeleton.md).
- **Вне очереди, найдено при чтении «Что открыто» messenger-skeleton.md (было
  п.6).** `POST /tasks` и `POST /messenger/dialogs/:id/create-task` в итоге
  вызывают один и тот же `CrmService.createTask`, а внутри него `operation`
  идемпотентности был захардкожен в `'createTask'` — один и тот же
  `Idempotency-Key`, отправленный на оба эндпоинта, ловил чужую запись и давал
  `IDEMPOTENCY_KEY_CONFLICT` (409) вместо двух независимых задач. Теперь
  `idempotencyOperation` — явный параметр, каждый вызывающий называет свою
  операцию (`'createTask'` без изменений у существующего эндпоинта,
  `'createTaskFromDialog'` — новое имя у messenger-пути). Подтверждено HTTP-
  тестом на настоящей MongoDB: общий ключ на обоих эндпоинтах создаёт две
  задачи (201/201, разные id), не 409. [messenger](operations/messenger-skeleton.md).
- **Вне очереди, найдено при чтении «Что открыто» messenger-skeleton.md (было
  п.3).** `sendMediaMessage` писал `media.assetId` в сообщение без проверки —
  та же дыра, что была у `link-crm` с `leadId`/`contactId`/`dealId`: можно
  было приложить к диалогу asset чужой организации, подобрав `ObjectId`.
  Теперь используется тот же cross-module accessor, что уже применяет
  `TeamService` для аватара позиции (`MediaService.getAssetForOwnerScope`):
  чужой/несуществующий asset — 404, неподтверждённый — 400. `url`-вложения
  без `assetId` (внешняя ссылка) не затронуты — отдельный, явно
  задокументированный вопрос продукта, не технический баг. Подтверждено HTTP-
  тестом на настоящей MongoDB. [messenger](operations/messenger-skeleton.md).
- **Вне очереди, найдено при чтении «Что открыто» messenger-skeleton.md (было
  п.5, вторая часть).** Удаление аккаунта мессенджера удаляло только сам
  аккаунт — диалоги и их сообщения оставались висеть с указателем на уже
  несуществующие `accountId`/`dialogId` (тот же класс проблемы, что чистка
  N-02 в community). Добавлен каскад в той же транзакции
  (`deleteByAccountId` → `deleteByDialogIds`), счётчики попадают в
  audit-запись. Подтверждено на настоящей MongoDB: диалоги и сообщения
  удалённого аккаунта пропадают, другой аккаунт той же организации и тот же
  `accountId` в чужой организации не затронуты.
  [messenger](operations/messenger-skeleton.md).
- **Вне очереди, найдено при чтении «Что открыто» messenger-skeleton.md (было
  п.4).** Два расхождения с уже опубликованным OpenAPI (implementation
  отставала от спеки): `GET /dialogs/:id/messages` отдавал голый массив —
  приведено к `{items, nextCursor}` (тот же `limit+1` приём, что у списка
  диалогов; курсор остался простым `_id` — сейчас безопасно, сортировка и
  `_id` согласованы, пока нет независимой простановки даты сообщений);
  `link-crm` отвечал 201 вместо 200 — добавлен `@HttpCode(200)` (обновляет
  диалог, не создаёт ресурс). Подтверждено HTTP-тестом на настоящей
  MongoDB. [messenger](operations/messenger-skeleton.md).
- **Вне очереди, найдено при чтении «Что открыто» billing-manual-
  subscriptions.md (было п.4).** `GET /admin/organizations/:id/billing`
  (обзор тарифа/подписки/журнала организации в админке) защищал только факт
  входа администратора — та же дыра, что была у активации тарифа до 10.09:
  любой админ с любым, даже не относящимся к биллингу грантом мог посмотреть
  финансовый обзор любой организации. Теперь нужен грант `manual_ledger.read`
  (зеркалит уже существующий `manual_ledger.write` у активации; в матрицу
  добавлена отсутствовавшая строка «Manual ledger read»).
  [billing](operations/billing-manual-subscriptions.md).
- **Вне очереди, найдено при чтении «Что открыто» billing-manual-
  subscriptions.md (было п.3).** Первая подписка организации хардкодила
  `planCode: 'agency_trial'` для ЛЮБОЙ организации без подписки, включая
  застройщика — тот получал бы agency-лимиты (`maxActiveListings:30`)
  вместо `developer_trial` (лимит 20, свой набор фич). Код плана теперь
  выбирается по реальному типу организации: `developer` → `developer_trial`,
  `agency` → `agency_trial` (без изменений), `independent_realtor` →
  `realtor_free` (в каталоге нет trial-плана для риэлтора). Длительность
  триала (14 дней для всех) и то, что `GET /billing/subscription` создаёт
  запись при чтении, — сознательно не тронуты, остаются открытыми вопросами.
  Подтверждено на настоящей MongoDB (реальный `DEFAULT_PLANS`, не мок).
  [billing](operations/billing-manual-subscriptions.md).
- **Вне очереди, найдено при чтении «Что открыто» billing-manual-
  subscriptions.md (было п.4, первая половина).** Ответ журнала
  (`GET /billing/ledger` и `ledger` внутри admin-обзора) отдавал сырой
  Mongoose `_id`, хотя OpenAPI уже объявляет `id` как часть контракта —
  тот же класс расхождения, что уже чинили в messenger. Добавлен маппер в
  read-модель. `reason`/`recordedBy` НЕ тронуты: тот же OpenAPI-контракт уже
  явно объявляет их видимыми владельцу — оказалось не багом кода, а
  вопросом, было ли само это решение в контракте продуманным (остаётся в
  «Что открыто» уже как вопрос владельцу/продукту, не техническая задача).
  [billing](operations/billing-manual-subscriptions.md).
- **Вне очереди, найдено при чтении «Что открыто» billing-manual-
  subscriptions.md (было п.5, последний механический пункт модуля).**
  admin-web (`OrganizationBillingModal.tsx`) держал свою копию каталога
  тарифов руками — разошёлся бы с `DEFAULT_PLANS` backend'а при следующем
  изменении цен. Тенантский `GET /billing/plans` не подходит (гейтится
  `TenantGuard`, у admin-сессии нет organizationId/positionId) — добавлен
  новый `GET /admin/billing/plans` (только `AdminGuard`, каталог не
  organizationId-специфичен), модалка теперь берёт code/name/price/audience
  оттуда. `periodDays`-по-умолчанию при выборе плана остался локальной
  UX-подсказкой — backend не хранит такого поля у плана вообще, так что
  разойтись с "правдой" ему физически не с чем. Тесты на обеих сторонах
  (api: делегирование в `BillingService.listPlans`; admin-web: тот же
  `strictFetcher`-паттерн non-mock-data, что уже используется для проверки
  publications-флоу). После этого пункта в billing-manual-subscriptions.md
  остаются только вопросы, требующие решения владельца/продукта.
  [billing](operations/billing-manual-subscriptions.md).
- **Вне очереди, найдено при чтении docs/operations/*.md на предмет
  оставшихся открытых пунктов за пределами уже пройденных сегодня модулей
  (седьмое наблюдение аудита из task-model-audit-followup.md, не вошедшее в
  таблицу трёх применённых вердиктов 02.09).** Видимость личных задач
  (`taskCategory: 'personal'`) держал только клиент — сервер отдавал личные
  задачи любого сотрудника целиком любому organization-scope гранту
  (`task.read` у owner/administrator/director). Утечка касалась не только
  `GET /tasks`, но и объединённого календаря и XLSX-выгрузки задач (выгрузка
  даже опаснее — постоянный файл на диске). Правило теперь в самом
  Mongo-запросе (`TaskRepository.listForOrganization`/
  `findByIdForOrganization`), не в контроллере поверх готового списка.
  Own-scope конкретной personal-задачи на запись (update/complete/reassign)
  сознательно не тронут — отдельный, ещё не закрытый вопрос. Подтверждено
  HTTP-тестом на настоящей MongoDB с реальными PermissionGrant-документами.
  [task-model-audit-followup](operations/task-model-audit-followup.md).
