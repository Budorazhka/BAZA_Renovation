# BAZA.sale — Figma-only UI Delivery Gate

Статус: **обязательно для каждого marketplace UI-изменения с 26.08.2026**.

## Зачем это нужно

Marketplace переписывается по Figma. Цель этого документа — не допустить generic-интерфейсов, переносов визуального мусора со старого сайта и «логичных» дизайнерских догадок исполнителя.

Визуальная правда marketplace: Figma.  
Поведение, не отражённое в Figma: решение владельца и принятые продуктовые документы.  
Старый сайт: только инвентарь функций и SEO, не образец UI.

## Обязательная последовательность

### 1. До кода

Для конкретного route исполнитель создаёт запись в таблице ниже и заполняет все поля:

1. Figma URL и точный node ID конкретного фрейма.
2. Имя фрейма, его desktop/mobile-варианты и интерактивные состояния.
3. Список компонентных вариантов, которые реально используются на экране.
4. Привязку к файлам реализации.
5. Явные Figma gaps: отсутствующие loading/empty/error/forbidden/conflict-состояния, спорные варианты, `(не используется)`.

Без этой записи UI-код не начинается.

### 2. Во время реализации

- Снимать размеры, сетку, отступы, typography, цвета, радиусы, границы и hover/focus/disabled из Figma или Figma component/variable styles.
- Реализовывать только подтверждённые блоки и взаимодействия.
- Не переносить HTML/CSS/визуальный паттерн со старого `baza.sale`.
- Не создавать "улучшающий" hero, выдуманные карточки, декоративные иллюстрации, fake stock imagery, фейковые метрики или несуществующие фильтры.
- Не применять Hallmark, шаблонные UI-подходы или произвольную новую палитру как замену Figma. Они допустимы только для технической реализации уже подтверждённого Figma-решения и не могут менять композицию.
- Если данных API не хватает для Figma-блока, зафиксировать backend gap; не скрывать его placeholder-данными и не менять дизайн под ограничение API без решения владельца.

### 3. Перед сдачей

1. Снять screenshot реализации в размерах соответствующего Figma-фрейма.
2. Сверить визуально с Figma: layout, header/footer, сетка, размеры карточки, цвета, шрифты, states, mobile.
3. Записать результат сверки и известные backend/Figma gaps в таблицу.
4. Проверить минимум desktop и Figma mobile-вариант. Если mobile в Figma отсутствует — явно отметить это.
5. Только после этого статус может стать `ready for owner review`.

## Definition of Done для marketplace UI

- [ ] Есть точный Figma node ID и ссылка.
- [ ] Подтверждены используемые desktop/mobile-фреймы.
- [ ] Нет выдуманной визуальной структуры или дизайна.
- [ ] Все подтверждённые Figma states реализованы.
- [ ] Отсутствующие в Figma states отмечены как gap и согласованы/следуют принятому DoD.
- [ ] Screenshot implementation сопоставлен с Figma.
- [ ] Функциональные API-gaps вынесены отдельным списком, а не замаскированы.
- [ ] Typecheck/test/build пройдены.

## Реестр реализации

| Route / feature | Figma URL + node ID | Desktop | Mobile | Целевые файлы | Подтверждённые states | Figma/API gap | Visual compare | Статус |
|---|---|---|---|---|---|---|---|---|
| Marketplace catalogue + development detail — текущий `apps/marketplace-web` scaffold | **Не привязан** | Не проверено | Не проверено | `baza-platform/apps/marketplace-web/**` | Только технические loading/error/empty | UI не соответствует Figma; API не отдаёт медиа, карту и `priceFrom` | Не выполнять как Figma compare | **BLOCKED — rewrite from exact Figma frames** |
| Поиск/карты/карточки объектов | `3314:195935` — "карточки объектов и поиск" | В inventory подтверждены full-width и open-map | В inventory подтверждены `mob_object`/`mob_project` | Ещё не назначены | Нужно извлечь из конкретных дочерних фреймов | Есть два фрейма `(не используется)` — не выбирать без решения владельца | Не начато | Ready for Figma extraction |

## Открытый запрос владельцу (от 04.09.2026)

Этот раздел существует потому, что гейт нельзя пройти в одиночку: четыре пункта
ниже — решения, которые по правилам самого гейта исполнителю принимать
запрещено. Пока на них нет ответа, marketplace UI остаётся в статусе `scaffold`,
и никакая работа по вёрстке не может быть начата — не из осторожности, а
буквально: без node ID нечего верстать.

Запрос стоит первым в очереди намеренно. Он единственный упирается в человека,
а не в код: остальные работы (backend, ERP, инфраструктура) идут параллельно и
его не ждут.

| # | Что нужно от владельца | Почему без этого нельзя начать | Что уже известно |
| --- | --- | --- | --- |
| 1 | **Node ID каждого экрана, который идёт в работу**, а не только секции. Нужны конкретные дочерние фреймы: карточка в списке, детальная страница, попап быстрого просмотра, фильтры — по каждому типу объекта (ЖК, квартира в новостройке, квартира вторичка, дом, участок, коммерция, аренда). | §1 гейта: «Одного общего `figma-screen-inventory.md` недостаточно для реализации экрана». Известен ровно один ID секции — `3314:195935`, внутри которой 113 узлов. | Секция «карточки объектов и поиск» разведена на «среднюю карточку» и «целую страницу» по 6–7 типам объектов; каждый тип — отдельный `COMPONENT_SET`. |
| 2 | **Решение по двум фреймам, помеченным `(не используется)`**: `ЖК целая страница` и `Проект целая страница`. Они актуальны, заменены или мертвы? | §«Если Figma содержит несколько вариантов либо фрейм помечен `(не используется)`, выбор нельзя угадывать». Это ровно детальные страницы ЖК и проекта — то есть основной экран каталога. | Дизайнер пометил их внутри самой Figma. `figma-screen-inventory.md` §114 фиксирует их вне build-scope до подтверждения. |
| 3 | **Состояния, которых в Figma нет**: loading, empty, error, forbidden, conflict. Согласовать минимальный системный вариант либо подтвердить, что их рисуют. | §«Если в Figma нет нужного состояния, исполнитель фиксирует Figma gap и предлагает минимальное системное состояние. Кодить его можно только после явного решения владельца либо существующего принятого правила DoD». | В текущем scaffold есть только технические loading/error/empty, не согласованные ни с чем. |
| 4 | **Подтверждение объёма первой волны**: какие экраны переписываются первыми. | Реестр ниже помечает текущий scaffold как `BLOCKED — rewrite from exact Figma frames`, то есть переписывание, а не доводка отступов. Порядок переписывания — продуктовое решение. | Кандидаты по инвентарю: каталог (список + карта), детальная карточка объекта, попап быстрого просмотра, фильтры. |

**Отдельно, чтобы не было недоразумения на приёмке.** Текущий `apps/marketplace-web`
не «почти готов, осталось причесать». По реестру ниже он подлежит переписыванию
с точных фреймов. Оценки готовности marketplace, посчитанные по функциональности
(поиск работает, карта работает, форма раскрытия контакта работает), к этому
гейту отношения не имеют: гейт про вёрстку, и по нему сделано ноль экранов.

## Формат отчёта исполнителя

Вместо фразы «сделал страницу» вернуть только:

1. Figma refs: URL + node IDs.
2. Реализованные файлы.
3. Что совпало с Figma.
4. Какие states/детали отсутствуют в Figma и как они были согласованы.
5. Screenshot paths и результат visual compare.
6. Backend gaps, которые мешают точному переносу.
