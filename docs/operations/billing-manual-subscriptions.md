# Биллинг: тарифы, подписки, ручная активация (этап 9, частично)

Дата: 11.09.2026. Код — `e501cb1` (09.09), исправления — `9925a5b`, `4c85cd7` (10.09).
Статус: частично. Каталог, подписка, журнал и ручная активация работают.
Тарифные лимиты и заморозка ничего не ограничивают.

## Что сделано

- Каталог `DEFAULT_PLANS` в `billing.service.ts`: семь планов — trial/standard/pro
  для застройщика, trial/pro для агентства, free/pro для риэлтора. Цены в USD,
  minor units. Засевается при первом обращении к каталогу.
- Подписка организации: `trial`, `active`, `grace_period`, `frozen`, `cancelled`.
  Истечение пересчитывается при чтении: 3 дня grace, затем `frozen`.
- Журнал (billing ledger): только добавление записей, с причиной и автором.
- Tenant-эндпоинты только на чтение. `GET /billing/plans` и
  `GET /billing/subscription` доступны любому сотруднику организации (только
  `TenantGuard`), `GET /billing/ledger` — грант `manual_ledger.read`, по матрице
  только owner.
- Админ: `GET /admin/organizations/:id/billing` — обзор,
  `POST /admin/organizations/:id/billing/activate` — ручная активация или продление.
- ERP: `TariffPage` и `BillingTab` читают реальные данные. admin-web:
  `OrganizationBillingModal`.

## Исправлено 10.09

- Активацию защищал только факт входа администратора: любой админ с любым
  грантом выдавал платный тариф за 0. Теперь нужен грант `manual_ledger.write`
  (permission-matrix §4, «Manual ledger change»).
- Повтор запроса продлевал подписку второй раз и писал вторую запись в журнал.
  Теперь обязателен `Idempotency-Key`, запись идемпотентности — в той же
  транзакции.
- `periodDays` ограничен 3650 (большие значения давали Invalid Date и 500),
  `currency` — только USD, GEL, RUB.

## Исправлено 11.09

- `GET /admin/organizations/:id/billing` (обзор тарифа/подписки/журнала)
  защищал только факт входа администратора — та же дыра, что была у активации
  до 10.09: любой админ с любым, даже не относящимся к биллингу грантом мог
  посмотреть финансовый обзор любой организации. Теперь нужен грант
  `manual_ledger.read` (permission-matrix §4, «Manual ledger read» — новая
  строка, до этой правки матрица вообще не определяла нужный грант на чтение).
  Юнит: `admin-billing.controller.spec.ts` (без гранта — исключение до вызова
  сервиса; с грантом — сервис вызывается с правильным organizationId).
- **Первая подписка организации получает trial-план её собственного типа, не
  всегда `agency_trial`.** `getOrganizationSubscription` при отсутствии
  подписки хардкодил `planCode: 'agency_trial'` для любой организации —
  застройщик получал бы `targetAudience:'agency'` и agency-лимиты
  (`maxActiveListings:30`) вместо `developer_trial` (лимит 20, свой набор
  фич). Теперь код плана выбирается по `Organization.type`
  (`OrganizationsService.getOrganizationById`, тот же cross-module accessor,
  что уже используют другие модули): `developer` → `developer_trial`,
  `agency` → `agency_trial` (без изменений), `independent_realtor` →
  `realtor_free` (в каталоге нет trial-плана для риэлтора — постоянно
  бесплатный тариф ближайший по смыслу вариант, не выдумка). Длительность
  триала (14 дней для всех, отдельно от выбора кода плана) и то, что чтение
  `GET /billing/subscription` при первом обращении создаёт запись, этой
  правкой намеренно не тронуты — остаются в «Что открыто» ниже. Юнит
  (`billing.service.spec.ts`) + реальная MongoDB
  (`billing-trial-plan-selection.integration-spec.ts` — реальный
  `DEFAULT_PLANS`, не мок; developer/agency/independent_realtor + повторный
  вызов не пересоздаёт подписку).
- **Ответ журнала (`getLedgerForOwner`/`ledger` внутри admin-обзора) теперь
  отдаёт `id`, не сырой Mongoose `_id`.** OpenAPI (`components.schemas.
  BillingLedgerEntry`) уже объявляет `id` как часть контракта — implementation
  отставала от спеки, тот же класс расхождения, что уже чинили в messenger.
  Добавлен `toBillingLedgerEntryReadModel` (ObjectId-поля → строки, `createdAt`
  → ISO-строка). `reason`/`recordedBy` при этом оставлены как есть намеренно:
  тот же OpenAPI-контракт уже явно объявляет оба поля видимыми владельцу — это
  не тот же класс проблемы, что `_id`/`id`, и не мне решать задним числом, что
  уже формально задокументированный контракт был ошибкой (см. п.4 ниже —
  сузился до вопроса, была ли эта видимость на самом деле продуманным
  решением). Юнит: `billing.service.spec.ts` (оба места, `_id` в ответе
  отсутствует, ObjectId/Date-поля — строки).
- **admin-web больше не держит свою копию каталога тарифов.**
  `OrganizationBillingModal.tsx` хардкодил `AVAILABLE_PLANS` (code/name/
  defaultDays/price для всех семи планов) — неизбежно разошёлся бы с
  `DEFAULT_PLANS` backend'а при следующем изменении цен/лимитов. Тенантский
  `GET /billing/plans` для этого не подходит: он защищён `TenantGuard`
  (organizationId/positionId), а у admin-сессии их нет вовсе (другая модель
  аутентификации). Добавлен новый `GET /admin/billing/plans`
  (`AdminBillingPlansController`, только `AdminGuard` — каталог планов не
  organizationId-специфичен и не финансовая история конкретной организации,
  тот же уровень доступа, что у тенантского эндпоинта). Модалка теперь
  запрашивает каталог и берёт code/name/price/audience оттуда; локально
  остался только `periodDays`-по-умолчанию при выборе плана — backend не
  хранит "срок по умолчанию" при плане вообще (`periodDays` — свободное поле
  формы активации), так что это не тот же класс дублирования: значение
  физически не может разойтись с "правдой" backend'а, у backend'а такой
  правды нет. Отображаемое название плана — `name` из каталога плюс перевод
  `targetAudience` через уже существующий `organizationTypeLabel`, не новая
  таблица переводов (старые русские названия тиров вроде «Пробный»/«Профи» не
  воспроизведены — это было цельное название, а не то, что можно честно
  собрать из полей реального каталога).
  Тесты: `admin-billing-plans.controller.spec.ts` (делегирование в
  `BillingService.listPlans`), `organizationBillingModal.test.tsx`
  (admin-web, тот же `strictFetcher`-паттерн, что `no-mock-data.test.tsx` —
  дропдаун плана рисуется из ответа `/admin/billing/plans`, не из
  локального списка).

## Почему так

Оплата пока ручная: мастер-план, этап 9 — «Telegram/manual payment request с
административным подтверждением». Поэтому единственная мутация — активация
администратором с обязательной причиной. Организация сама свою подписку не меняет.

## Что открыто

1. **Лимиты тарифов не применяются.** `effectiveLimits` (объявления, должности,
   доступ к CRM, шахматке, лендингу) вычисляется и отдаётся, но ни публикация, ни
   создание должности его не проверяют. Этап 9 требует серверных entitlement-ов.
2. **`frozen` ничего не блокирует** — ни у организации (заморозка в админке), ни у
   подписки. Предупреждение в admin-web обещает блокировку действий и скрытие
   публикаций; до реализации это неправда. Что именно должна блокировать
   заморозка — решение владельца.
3. **Длительность trial не совпадает с мастер-планом** (правильный план по типу
   организации теперь выбирается, см. выше). Этап 9: «согласованный 7-дневный
   trial», код даёт 14 дней всем — сменить длительность без подтверждения от
   владельца, что 14 дней не намеренное расхождение с устаревшей записью
   мастер-плана, не стал. Отдельно: `GET /billing/subscription` при первом
   обращении создаёт запись подписки — чтение меняет данные (осознанный ленивый
   provisioning или нет — тоже не решал сам).
4. **Владелец организации видит в журнале внутреннюю причину администратора и
   `recordedBy`** (`_id`/`id` в ответе исправлено, см. выше). Это уже
   формально часть OpenAPI-контракта (`BillingLedgerEntry.reason`/
   `.recordedBy` объявлены, не случайная утечка на уровне кода) — вопрос не
   «баг ли это», а было ли решение сделать эту видимость частью контракта
   продуманным (прозрачность для владельца, кто и почему менял его тариф)
   или это сам контракт был написан не подумав. Решение владельца/продукта,
   не техническая правка.
5. Нет разделения plan / entitlement / usage / promotion credit из этапа 9;
   hosted landing — только флаг `landingAccess`.
